{
  "name": "cbor-devtools-extension",
  "version": "1.0.0",
  "description": "DevTools extension that auto-decodes Smithy RPC v2 CBOR (`application/cbor`) responses. Works in both Firefox and Chrome.",
  "main": "cbor.js",
  "scripts": {
    "test": "vitest run",
    "prepare": "git config core.hooksPath .githooks",
    "build:firefox": "web-ext build --source-dir . --artifacts-dir dist --ignore-files test/ node_modules/ package.json package-lock.json .git .gitignore .githooks dist/",
    "publish:firefox": "node scripts/publish.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vitest": "^4.1.9",
    "web-ext": "^10.4.0"
  }
}
